import{_ as e,c as s,o as t,ag as i}from"./chunks/framework.DEnUjgmn.js";const g=JSON.parse('{"title":"⬆️ Upgrading from status-based publication","description":"","frontmatter":{},"headers":[],"relativePath":"Upgrading/index.md","filePath":"Upgrading/index.md"}'),n={name:"Upgrading/index.md"};function o(l,a,d,r,p,h){return t(),s("div",null,[...a[0]||(a[0]=[i(`<div style="display:none;" hidden="true" aria-hidden="true">Are you an LLM? You can read better optimized documentation at /camomilla-core/Upgrading.md for this page in Markdown format</div><h1 id="⬆️-upgrading-from-status-based-publication" tabindex="-1">⬆️ Upgrading from status-based publication <a class="header-anchor" href="#⬆️-upgrading-from-status-based-publication" aria-label="Permalink to &quot;⬆️ Upgrading from status-based publication&quot;">​</a></h1><p>Older camomilla releases (<strong>≤ 6.4</strong>) stored a page&#39;s publication state in two columns:</p><ul><li><code>status</code> — a translatable <code>CharField</code> (<code>PUB</code> / <code>DRF</code> / <code>PLA</code> / <code>TRS</code>)</li><li><code>publication_date</code> — a global <code>DateTimeField</code></li></ul><p>The new lifecycle <strong>derives</strong> that state from timestamps instead:</p><ul><li><code>published_at</code> — translatable; when <em>this language</em> went / goes live</li><li><code>deleted_at</code> — global soft-delete marker</li><li>a separate <a href="./../How to/Use Page Lifecycle/"><code>Draft</code></a> table (no old equivalent — drafts simply start empty)</li></ul><p>This page is <strong>only</strong> relevant if you&#39;re upgrading an existing project that already has data in the old <code>status</code> / <code>publication_date</code> columns. New installs need nothing here.</p><div class="danger custom-block"><p class="custom-block-title">Back up your database first</p><p>This migration drops the old columns. Take a full backup (or snapshot) before you start. The data step is <strong>forward-only</strong> — to roll back you restore the backup.</p></div><h2 id="what-the-data-step-does" tabindex="-1">What the data step does <a class="header-anchor" href="#what-the-data-step-does" aria-label="Permalink to &quot;What the data step does&quot;">​</a></h2><p>For every concrete page model (<code>Page</code>, <code>Article</code>, and any custom <code>AbstractPage</code> subclass), per language:</p><table tabindex="0"><thead><tr><th>Old <code>status</code></th><th>New <code>published_at</code></th></tr></thead><tbody><tr><td><code>PUB</code> Published</td><td><code>publication_date</code> if it&#39;s in the past, otherwise <em>now</em> (stays live)</td></tr><tr><td><code>PLA</code> Planned</td><td><code>publication_date</code> verbatim — a future date stays <code>PLA</code>, a past date becomes <code>PUB</code>, an empty one becomes <code>DRF</code></td></tr><tr><td><code>DRF</code> Draft</td><td><code>NULL</code> (draft)</td></tr><tr><td><code>TRS</code> Trashed</td><td><code>NULL</code> (hidden)</td></tr></tbody></table><p><code>deleted_at</code> is <strong>global</strong> now, so it&#39;s set only when <strong>every</strong> language of a page is <code>TRS</code>. A page trashed in one language but live in another keeps its live languages; the trashed language just becomes &quot;not published&quot;. The mapping preserves the old <code>is_public</code> result exactly for every combination (there&#39;s a test pinning this).</p><h2 id="procedure" tabindex="-1">Procedure <a class="header-anchor" href="#procedure" aria-label="Permalink to &quot;Procedure&quot;">​</a></h2><h3 id="_1-upgrade-the-package" tabindex="-1">1. Upgrade the package <a class="header-anchor" href="#_1-upgrade-the-package" aria-label="Permalink to &quot;1. Upgrade the package&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -U</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> django-camomilla-cms</span></span></code></pre></div><p>Add <code>django-reversion</code> to <code>INSTALLED_APPS</code> (new hard dependency that powers page revisions):</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">INSTALLED_APPS</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # …</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;reversion&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span></code></pre></div><h3 id="_2-generate-the-migration-—-with-the-data-step-already-inserted" tabindex="-1">2. Generate the migration — with the data step already inserted <a class="header-anchor" href="#_2-generate-the-migration-—-with-the-data-step-already-inserted" aria-label="Permalink to &quot;2. Generate the migration — with the data step already inserted&quot;">​</a></h3><p>Use camomilla&#39;s <code>camomilla_makemigrations</code> command instead of <code>makemigrations</code>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> manage.py</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> camomilla_makemigrations</span></span></code></pre></div><p>It&#39;s a drop-in wrapper around <code>makemigrations</code> (same flags — <code>--dry-run</code>, <code>--name</code>, …) that runs camomilla&#39;s migration injectors over the generated migrations and <strong>auto-inserts the matching data step</strong> in the correct position whenever it recognises a breaking change — here, the status → lifecycle transition. (It&#39;s a general mechanism: future camomilla upgrades register their own injectors, so the same command keeps handling them.) You get a ready-to-apply migration — no hand-editing. It prints a line when it injects:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>  + auto-inserted MigrateStatusToLifecycle(page) into the camomilla migration</span></span>
<span class="line"><span>  + auto-inserted MigrateStatusToLifecycle(article) into the camomilla migration</span></span></code></pre></div><p>(one per page model — your own apps&#39; custom page models get their own, in their own migrations)</p><div class="tip custom-block"><p class="custom-block-title">Don&#39;t pass an app name</p><p>Run it <strong>without</strong> an app argument. The transition affects <code>camomilla.Page</code> / <code>camomilla.Article</code> <strong>and</strong> any custom <code>AbstractPage</code> subclass in your own apps — whose migrations are generated in <em>your</em> app, not in <code>camomilla</code>. A no-arg run injects the data step into every affected app&#39;s migration in one pass; <code>… camomilla</code> would skip your custom page models. Add <code>--dry-run</code> to preview without writing.</p></div><div class="warning custom-block"><p class="custom-block-title">Already ran plain <code>makemigrations</code>?</p><p>If you ran the stock <code>python manage.py makemigrations</code> <strong>before</strong> this step, it wrote a migration that drops <code>status</code> with <strong>no backfill</strong> in between — applying it would lose your publication state. Delete that just-generated migration and regenerate it with <code>camomilla_makemigrations</code> (the injector only rewrites migrations as they&#39;re generated, not ones already on disk). The data step <em>is</em> re-inserted automatically if the lifecycle columns were added in one migration and the legacy ones are dropped in a later one — but a single migration that does both without the backfill is unsafe to apply.</p></div><details class="details custom-block"><summary>Prefer to do it by hand?</summary><p>Run the normal <code>python manage.py makemigrations</code>, then open each generated migration and add <strong>one operation per page model</strong>, naming the model. Place each <code>MigrateStatusToLifecycle(&quot;&lt;model&gt;&quot;)</code> <strong>after</strong> that model&#39;s <code>AddField</code> ops (<code>published_at*</code> / <code>deleted_at</code>) and <strong>before</strong> its <code>RemoveField</code> ops (<code>status*</code> / <code>publication_date</code>).</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> camomilla.upgrades.migrations </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> MigrateStatusToLifecycle</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">operations </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    migrations.AddField(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;page&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;published_at&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">),       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># + per-language, deleted_at</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    migrations.AddField(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;article&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;published_at&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    MigrateStatusToLifecycle(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;page&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">),                        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># one op per model</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    MigrateStatusToLifecycle(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;article&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    migrations.RemoveField(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;page&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;status&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">),               </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># + per-language, publication_date</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    migrations.RemoveField(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;article&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;status&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span></code></pre></div><p>Each op migrates only its own model, so the same operation appears safely in several apps&#39; migrations (yours, for custom page models, plus camomilla&#39;s) without overlapping.</p></details><h3 id="_3-apply-it" tabindex="-1">3. Apply it <a class="header-anchor" href="#_3-apply-it" aria-label="Permalink to &quot;3. Apply it&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> manage.py</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> migrate</span></span></code></pre></div><h3 id="_4-verify" tabindex="-1">4. Verify <a class="header-anchor" href="#_4-verify" aria-label="Permalink to &quot;4. Verify&quot;">​</a></h3><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> camomilla.models </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Page</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Page.objects.public().count()    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># pages that were PUB (or PLA whose date has passed)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Page.objects.trashed().count()   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># pages that were TRS in every language</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Page.objects.draft().count()     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># (drafts start empty — this counts pending Draft rows)</span></span></code></pre></div><p><code>Page.objects.filter(status=&quot;PUB&quot;)</code> still works too — the manager rewrites derived-status lookups into timestamp conditions, so most existing query code keeps running unchanged. See <a href="./../How to/Use Page Lifecycle/">Use Page Lifecycle</a>.</p><h2 id="notes" tabindex="-1">Notes <a class="header-anchor" href="#notes" aria-label="Permalink to &quot;Notes&quot;">​</a></h2><ul><li><strong>Drafts start empty.</strong> The old system had no draft storage, so there&#39;s nothing to backfill into the <code>Draft</code> table. The draft / preview / scheduling workflow is available immediately for new edits.</li><li><strong>One-way.</strong> The data step&#39;s reverse is a no-op (<code>migrations.RunPython.noop</code>) — rolling the migration back restores the columns but not their values. Restore from your backup if you need to revert.</li><li><strong>Custom page models</strong> are handled automatically — the transform runs against every model that carries both the old <code>status</code> and new <code>published_at</code> columns at migration time.</li></ul>`,33)])])}const k=e(n,[["render",o]]);export{g as __pageData,k as default};
